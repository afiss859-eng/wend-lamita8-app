// ⚠️  Ce fichier est généré par `npm run build`
// ⚠️  Lancez `npm run build` après avoir configuré votre .env
// ⚠️  Ne jamais éditer manuellement ni committer ce fichier

window.APP_CONFIG = {
  ANTHROPIC_API_KEY: "",
  APP_NAME: "Wend-Lamita",
  APP_SHOP: "Bobo-Dioulasso",
  APP_VERSION: "1.0.0",
  BUILD_DATE: ""
};

if (!window.APP_CONFIG.ANTHROPIC_API_KEY) {
  console.warn("[Wend-Lamita] Clé API manquante. Lancez `npm run build` après avoir configuré .env");
}
